/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/RealTimeStudentSuccessPredictionSystem'
}

module.exports = nextConfig 